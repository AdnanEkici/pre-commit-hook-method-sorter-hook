# method-sorter-hook
method-sorter-hook is a customizable Python pre-commit hook for enforcing a consistent method order inside classes. It can organize dunder methods, properties, abstract methods, decorated methods, and public/protected/private methods according to project-defined rules.
